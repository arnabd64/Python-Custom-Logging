[project]
name = "python-custom-logging"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy>=2.0.41",
]

[dependency-groups]
dev = [
    "isort>=6.0.1",
]
